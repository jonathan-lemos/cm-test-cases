int x;
int x(int q) { {int q; q = q; return x + q + x(x);} }
void main(void) {
    int q;
    int x;
    if (x(x) == x(q)) {
        int x = x(x);
        x + q;
    }
    else if (1.0 == 1.0) {
        x + q;
    }
    x = x + x + q + q + x(q);
}
