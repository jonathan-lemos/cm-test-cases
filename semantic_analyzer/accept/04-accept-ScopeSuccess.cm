float a;
int x;
int x(int q) { { int q; { q = q; { return x + q + x(x); } } } }
void main(void) {
    int q;
    int x;
	float b;
    if (x(x) == x(q)) {
		float c;
        int x;
		c = a + b;
        x = x(x);
        x + q;
    }
    else if (1.0 == 1.0) {
        x + q;
    }
    x = x + x + q + q + x(q);
}
